FROM postgres:17-alpine
RUN apk add --no-cache curl

# Set labels for better image management
LABEL maintainer="yohan@yohanzbinden.ch"
LABEL description="PostgreSQL 17 with curl for backup operations"
LABEL version="17.0"

